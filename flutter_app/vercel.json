{
    "buildCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter create . --platforms=web && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release --dart-define=API_BASE_URL=${API_BASE_URL}",
    "outputDirectory": "build/web",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}